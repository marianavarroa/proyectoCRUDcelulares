export class Producto {
    constructor(codigo, producto, descripcion, cantidad, url, precio) {
        this.codigo = codigo;
        this.producto = producto;
        this.descripcion = descripcion;
        this.cantidad = cantidad;
        this.url = url;
        this.precio = precio;
    }
}